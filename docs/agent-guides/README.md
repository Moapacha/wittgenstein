# Agent Guides

This folder holds short, phase-specific guides that can be handed directly to coding agents.

Use an agent guide when:

- the doctrine is already decided,
- the remaining work is implementation sequencing,
- a contributor needs a reliable starting prompt and guardrails.

Each guide should be:

- narrowly scoped,
- tied to one phase or one migration,
- explicit about invariants,
- explicit about out-of-scope work,
- readable without prior chat context.

Current guides:

- `image-to-audio-port.md` — the first concrete execution line after the v0.2 lock.
