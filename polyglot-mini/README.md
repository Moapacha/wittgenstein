# polyglot-mini

Experimental **Code-as-Painter** stack (Python). Large COCO/Flickr caption JSON files under `train/coco/*.json` are **not** committed (see repo root `.gitignore`); download or generate them locally if you run `build_dataset_coco.py`.
