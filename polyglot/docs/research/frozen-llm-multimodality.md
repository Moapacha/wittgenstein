# Frozen LLM Multimodality

The repo assumes multimodality can be achieved by pairing a strong text model with modality-specific interfaces, decoders, and small adapters instead of retraining an end-to-end frontier model.
