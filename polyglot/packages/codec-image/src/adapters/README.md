# Image Adapters

This folder is reserved for the small trained component that maps scene-spec semantics to discrete latent tokens expected by the frozen decoder.

Expected future contents:

- model loader
- tokenizer / codebook mapping utilities
- adapter checkpoint resolver
- evaluation helpers for latent alignment
